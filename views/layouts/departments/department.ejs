<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= department.name %></title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>

<header class="page-header">
    <h1><%= department.name %></h1>
    <p class="subtitle">Laboratory Department Details</p>
</header>

<main class="container">
    <a href="/" class="back-link">← Back to Home</a>


    <section class="details-card">

        <div class="details-row">
            <span class="label">Department Code</span>
            <span class="value"><%= department.code_name %></span>
        </div>

        <div class="details-row">
            <span class="label">Description</span>
            <span class="value"><%= department.description %></span>
        </div>

        <div class="details-row">
            <span class="label">Status</span>
            <span class="status <%= department.is_active ? 'active' : 'inactive' %>">
                <%= department.is_active ? "Active" : "Inactive" %>
            </span>
        </div>

        <div class="details-row">
            <span class="label">Created</span>
            <span class="value"><%= department.created_at %></span>
        </div>

        <div class="details-row">
            <span class="label">Last Updated</span>
            <span class="value"><%= department.updated_at %></span>
        </div>

    </section>

</main>

</body>
</html>
