<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%=department.name%> department </title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <header class="page-header">
    <h1>Edit <%=department.name%> Department</h1>
    <p class="subtitle">Edit and submit the details of <%=department.name%> Department</p>
</header>
    <section class="form-card">
        <form action="" method="POST" class="lab-form">
            <div class="form-group">
                <label for="department_name">Name</label>
                <input type="text" value="<%=department.name%>" id="department_name" name="department_name">
            </div>
            <div class="form-group">
                <label for="department_code_name">Code Name</label>
                <input type="text" name="department_code_name" id="department_code_name" value="<%=department.code_name%>">
            </div>
            <div class="form-group">
                <label for="department_description">Description</label>
                <textarea name="department_description" id="department_description"><%= department.description || "" %></textarea>
            </div>
            <div class="form-group">
                <label for="">Is Active</label>
                <input type="checkbox" name="is_active" value="true" <%= department.is_active ? "checked" : "" %>>
            </div>
            <button type="submit" class="primary-btn">Submit Edit</button>
        </form>
    </section>
</body>
</html>